# Keep the build context small. Only serve/, docker/ (incl. _stage), and
# requirements are needed; everything below is irrelevant to the runtime image.
.git
build/
**/build/
**/__pycache__/
**/*.pyc
docs/
tests/
examples/
host_toolchain/
*.hbm
*.gguf
*.bin
*.onnx
*.safetensors
*.tar.gz
*.zip
models/
D-Robotics_LLM_*/
